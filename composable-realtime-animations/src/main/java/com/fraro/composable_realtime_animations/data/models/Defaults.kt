package com.fraro.composable_realtime_animations.data.models

import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.graphics.Color
import com.fraro.composable_realtime_animations.data.models.Shape.Ellipse

const val rotationDefault = 0F
val colorDefault = Color.Blue
val shapeDefault = Ellipse()
val offsetDefault = Offset(0F,0F)
